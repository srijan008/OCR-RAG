{
  "name": "ocrtorag-root",
  "version": "1.0.0",
  "description": "Master orchestration for OCR-to-RAG Pipeline",
  "scripts": {
    "install:all": "npm install --prefix frontend && pip install -r backend/requirements.txt",
    "backend": "cd backend && venv\\Scripts\\activate && uvicorn main:app --reload --port 8000",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
